# Sorter
My stash sorter script

## Usage goes about like this:
1. You need to use [`Kitty`](https://sw.kovidgoyal.net/kitty/) terminal emulator for the image function to work.
2. Put the script or a link of the script to a bin folder (I recommend `~/.local/bin/`)
3. run it
